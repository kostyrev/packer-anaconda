---
sudo: required

language: python

services:
  - docker

# branches:
#   only:
#   - master

before_install:
- sudo apt-get -qq update
- sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y docker-ce

install:
- pip install --quiet -r requirements.txt

script:
  - echo yaya

notifications:
  webhooks: https://chat.fasten.com/hooks/aafyrnmfitnwbpuxja4gzr19cr
